name: Hosted builds

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:
    inputs:
      product:
        description: Product to rebuild (all on ordinary push/PR)
        type: choice
        default: all
        options: [all, linux-host, linux-client, macos-host, macos-client]
      source_sha:
        description: Expected commit (optional in UI; required by the dispatch helper)
        type: string
      signed:
        description: Request protected macOS signing (allowed branches only)
        type: boolean
        default: false
      clean_bootstrap:
        description: Bypass dependency cache restore and save
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: hosted-builds-${{ github.ref }}-${{ inputs.product || 'all' }}
  cancel-in-progress: true

env:
  BUILD_REF: ${{ github.head_ref || github.ref_name }}
  PYTHONDONTWRITEBYTECODE: '1'
  EXPECTED_SOURCE_SHA: ${{ inputs.source_sha || github.sha }}

jobs:
  policy:
    runs-on: ubuntu-26.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - run: python3 -m unittest discover -s tests/ci -v
      - run: bash scripts/test/check-decode-contract.sh "$RUNNER_TEMP/decode-contract"
      - name: Check client compatibility and package gates
        run: |
          python3 tests/packaging/test-macos-client-target.py -v
          python3 tests/packaging/test-macos-minimum-os.py -v
          python3 tests/packaging/test-build-paths.py -v
      - name: Require the intended source revision
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_SOURCE_SHA"

  linux-host:
    if: ${{ !inputs.product || inputs.product == 'all' || inputs.product == 'linux-host' }}
    needs: policy
    runs-on: ubuntu-24.04
    container: rockylinux/rockylinux:9.7@sha256:53f4c6dcb34e1403bd93207351f0af9a593610faeb7165cb8a037346765199b0
    timeout-minutes: 180
    defaults:
      run:
        shell: bash
    env:
      PLANK_BUILD_JOBS: '4'
    steps:
      - name: Prepare checkout tools
        run: |
          # Pin repositories before the first transaction, not after checkout.
          dnf --disablerepo='*' \
            --repofrompath=ci-baseos,https://dl.rockylinux.org/vault/rocky/9.7/BaseOS/x86_64/os/ \
            --repofrompath=ci-appstream,https://dl.rockylinux.org/vault/rocky/9.7/AppStream/x86_64/os/ \
            --enablerepo=ci-baseos --enablerepo=ci-appstream \
            install -y git tar gzip python3 dnf-plugins-core
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - run: python3 scripts/ci/context.py
      - run: bash scripts/ci/install-linux-deps.sh host
      - run: bash scripts/ci/bootstrap.sh linux-host
      - run: bash scripts/ci/build.sh linux-host
      - uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: linux-host-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ env.PLANK_ARTIFACT_ROOT }}/
          if-no-files-found: error
          retention-days: 7
      - name: Report disk usage
        if: always()
        run: df -h

  linux-client:
    if: ${{ !inputs.product || inputs.product == 'all' || inputs.product == 'linux-client' }}
    needs: policy
    runs-on: ubuntu-26.04
    timeout-minutes: 120
    env:
      PLANK_BUILD_JOBS: '4'
      QT_QPA_PLATFORM: offscreen
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - run: python3 scripts/ci/context.py
      - run: bash scripts/ci/install-linux-deps.sh client
      - run: bash scripts/ci/bootstrap.sh linux-client
      - run: bash scripts/ci/build.sh linux-client
      - uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: linux-client-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ env.PLANK_ARTIFACT_ROOT }}/
          if-no-files-found: error
          retention-days: 7
      - name: Report disk usage
        if: always()
        run: df -h

  macos:
    if: ${{ !inputs.signed && (!inputs.product || inputs.product == 'all' || startsWith(inputs.product, 'macos-')) }}
    needs: policy
    runs-on: xcode-27
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        product: ${{ fromJSON(inputs.product == 'macos-host' && '["host"]' || inputs.product == 'macos-client' && '["client"]' || '["host", "client"]') }}
        target: ['26.0', '27.0']
        exclude:
          - product: host
            target: '26.0'
    env:
      PLANK_BUILD_JOBS: '3'
      PLANK_MACOS_CLIENT_TARGET: ${{ matrix.target }}
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Require the selected product's Apple toolchain
        env:
          PLANK_CI_MAC_PRODUCT: ${{ matrix.product }}
        run: |
          test "$(uname -m)" = arm64
          if [[ $PLANK_CI_MAC_PRODUCT == client ]]; then
            source scripts/build/macos-client-target.sh
            plank_macos_client_target
          else
            test "$(sw_vers -productVersion | cut -d . -f 1)" -ge 27
            test "$(xcrun --sdk macosx --show-sdk-version | cut -d . -f 1)" -ge 27
          fi
          command -v cmake
          command -v ninja
      - run: python3 scripts/ci/context.py
      - name: Select exact Mac Client dependency inputs
        if: ${{ matrix.product == 'client' && !inputs.clean_bootstrap }}
        id: deps-key
        run: python3 scripts/ci/cache-macos-client.py prepare
      - name: Restore Mac Client dependencies
        if: ${{ matrix.product == 'client' && !inputs.clean_bootstrap }}
        id: deps-cache
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5, Node24
        with:
          key: ${{ steps.deps-key.outputs.key }}
          path: ${{ steps.deps-key.outputs.paths }}
      - name: Verify exact restored dependencies
        if: ${{ steps.deps-cache.outputs.cache-matched-key != '' }}
        env:
          CACHE_KEY: ${{ steps.deps-key.outputs.key }}
          MATCHED_KEY: ${{ steps.deps-cache.outputs.cache-matched-key }}
        run: |
          test "$MATCHED_KEY" = "$CACHE_KEY"
          python3 scripts/ci/cache-macos-client.py verify --key "$CACHE_KEY"
      - run: bash scripts/ci/bootstrap.sh macos-${{ matrix.product }}
      - run: bash scripts/ci/build.sh macos-${{ matrix.product }}
      - name: Seal successful Mac Client dependencies
        if: ${{ matrix.product == 'client' && !inputs.clean_bootstrap && github.event_name != 'pull_request' && steps.deps-cache.outputs.cache-hit != 'true' }}
        env:
          CACHE_KEY: ${{ steps.deps-key.outputs.key }}
        run: python3 scripts/ci/cache-macos-client.py seal --key "$CACHE_KEY"
      - name: Save Mac Client dependencies
        if: ${{ matrix.product == 'client' && !inputs.clean_bootstrap && github.event_name != 'pull_request' && steps.deps-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5, Node24
        with:
          key: ${{ steps.deps-key.outputs.key }}
          path: ${{ steps.deps-key.outputs.paths }}
      - name: Explain unsigned build scope
        run: echo 'Unsigned compile/test only. No distributable macOS package, signing credentials, or hardware acceptance.' >> "$GITHUB_STEP_SUMMARY"
      - name: Report disk usage
        if: always()
        run: df -h

  macos-signed:
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.signed && (inputs.product == 'macos-host' || inputs.product == 'macos-client') }}
    needs: policy
    environment: macos-signing
    runs-on: xcode-27
    timeout-minutes: 120
    env:
      PLANK_CI_PRODUCT: ${{ inputs.product }}
      PLANK_BUILD_JOBS: '3'
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Validate target and clean source
        run: |
          case "$PLANK_CI_PRODUCT" in macos-host|macos-client) ;; *) exit 2;; esac
          test "$(uname -m)" = arm64
          test "$(sw_vers -productVersion | cut -d . -f 1)" -ge 27
          test "$(xcrun --sdk macosx --show-sdk-version | cut -d . -f 1)" -ge 27
          python3 scripts/ci/context.py
      - name: Select exact Mac Client dependency inputs
        if: ${{ inputs.product == 'macos-client' && !inputs.clean_bootstrap }}
        id: deps-key
        run: python3 scripts/ci/cache-macos-client.py prepare
      - name: Restore Mac Client dependencies
        if: ${{ inputs.product == 'macos-client' && !inputs.clean_bootstrap }}
        id: deps-cache
        uses: actions/cache/restore@caa296126883cff596d87d8935842f9db880ef25 # v5, Node24
        with:
          key: ${{ steps.deps-key.outputs.key }}
          path: ${{ steps.deps-key.outputs.paths }}
      - name: Verify exact restored dependencies
        if: ${{ steps.deps-cache.outputs.cache-matched-key != '' }}
        env:
          CACHE_KEY: ${{ steps.deps-key.outputs.key }}
          MATCHED_KEY: ${{ steps.deps-cache.outputs.cache-matched-key }}
        run: |
          test "$MATCHED_KEY" = "$CACHE_KEY"
          python3 scripts/ci/cache-macos-client.py verify --key "$CACHE_KEY"
      - name: Build, sign, notarize and verify
        env:
          PLANK_DEVELOPER_ID_APPLICATION_P12: ${{ secrets.PLANK_DEVELOPER_ID_APPLICATION_P12 }}
          PLANK_DEVELOPER_ID_INSTALLER_P12: ${{ secrets.PLANK_DEVELOPER_ID_INSTALLER_P12 }}
          PLANK_DEVELOPER_ID_APPLICATION_PASSWORD: ${{ secrets.PLANK_DEVELOPER_ID_APPLICATION_PASSWORD }}
          PLANK_DEVELOPER_ID_INSTALLER_PASSWORD: ${{ secrets.PLANK_DEVELOPER_ID_INSTALLER_PASSWORD }}
          PLANK_APPLE_ID: ${{ secrets.PLANK_APPLE_ID }}
          PLANK_APPLE_APP_PASSWORD: ${{ secrets.PLANK_APPLE_APP_PASSWORD }}
          PLANK_MACOS_TEAM_ID: ${{ vars.PLANK_MACOS_TEAM_ID }}
        run: python3 scripts/ci/sign-macos.py
      - name: Remove temporary signing material
        if: always()
        run: python3 scripts/ci/sign-macos.py --cleanup
      - name: Seal successful Mac Client dependencies
        if: ${{ success() && inputs.product == 'macos-client' && !inputs.clean_bootstrap && steps.deps-cache.outputs.cache-hit != 'true' }}
        env:
          CACHE_KEY: ${{ steps.deps-key.outputs.key }}
        run: python3 scripts/ci/cache-macos-client.py seal --key "$CACHE_KEY"
      - name: Save Mac Client dependencies
        if: ${{ success() && inputs.product == 'macos-client' && !inputs.clean_bootstrap && steps.deps-cache.outputs.cache-hit != 'true' }}
        uses: actions/cache/save@caa296126883cff596d87d8935842f9db880ef25 # v5, Node24
        with:
          key: ${{ steps.deps-key.outputs.key }}
          path: ${{ steps.deps-key.outputs.paths }}
      - uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6
        with:
          name: signed-${{ inputs.product }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ env.PLANK_ARTIFACT_ROOT }}/
          if-no-files-found: error
          retention-days: 7
